require 'rubygems'
require 'cucumber'
require 'cucumber/rake/task'

task :default do
  puts 'Default task does nothing for now'
end

Cucumber::Rake::Task.new(:test) do |t|
    t.profile = "html"
end


